# newaiworld-agentic-hr-policy-assistant
Agentic HR Policy Assistant using RAG + MCP + LLM agent
cat > README.md <<'EOF'
# Agentic HR Policy Assistant

## 1. Problem Statement

This project will implement a grounded HR policy assistant using retrieval-augmented generation, MCP-exposed tools, structured synthetic HR data, and an agent orchestrator.

## 2. Architecture Diagram

Architecture implementation is pending. The frozen design uses FastAPI, a manual agent orchestrator, an MCP client/server over stdio, Chroma, local embeddings, and mock JSON data.

## 3. Setup Instructions

Setup instructions will be expanded as each phase is implemented and verified.

## 4. Environment Variables

See `.env.example`.

## 5. Running Locally

Pending application implementation.

## 6. Running Tests

```bash
source .venv/bin/activate
pytest